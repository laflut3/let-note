apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
stringData:
  PS_BDD_PASS: "${PS_BDD_PASS}"
  POSTGRES_PASSWORD: "${PS_BDD_PASS}"
  JWT_SECRET: "${JWT_SECRET}"
  COOKIE_SECURE: "${COOKIE_SECURE}"
