apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: let-note-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/laflut3/let-note.git
    targetRevision: main
    path: infrastructure/deploiement-kube/helm/let-note
    helm:
      releaseName: let-note-dev
      valueFiles:
      - environments/dev.yaml
      parameters:
      - name: images.backend.tag
        value: __IMAGE_TAG__
      - name: images.frontend.tag
        value: __IMAGE_TAG__
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
